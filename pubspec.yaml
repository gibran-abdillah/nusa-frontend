name: nusa
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  camera: ^0.11.0
  fl_chart: ^1.1.1
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2
  http: ^1.6.0
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
